export interface GitlabConfig {
  id: string;
  gitlabURL: string;
  token: string;
  ignoreSSL: boolean;
}

// export function createGitlabConfig(params: Partial<GitlabConfig>) {
//   return {

//   } as GitlabConfig;
// }
